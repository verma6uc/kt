// Previous content remains the same until the notification model...

// New Monitoring Models
model user_activity {
  id            Int      @id @default(autoincrement())
  user_id       Int
  company_id    Int
  date          DateTime @db.Date
  session_count Int      @default(1)
  created_at    DateTime @default(now())

  company       company  @relation(fields: [company_id], references: [id], onDelete: Cascade)
  user          user     @relation(fields: [user_id], references: [id], onDelete: Cascade)

  @@index([company_id, date])
}

model feature_usage {
  id           Int      @id @default(autoincrement())
  company_id   Int
  feature_name String
  usage_count  Int      @default(1)
  created_at   DateTime @default(now())

  company      company  @relation(fields: [company_id], references: [id], onDelete: Cascade)

  @@index([company_id, feature_name])
}

model growth_metrics {
  id           Int      @id @default(autoincrement())
  company_id   Int
  active_users Int
  total_storage Float
  api_calls    Int
  created_at   DateTime @default(now())

  company      company  @relation(fields: [company_id], references: [id], onDelete: Cascade)

  @@index([company_id, created_at])
}

model error_logs {
  id            Int      @id @default(autoincrement())
  company_id    Int
  error_type    String
  error_message String
  stack_trace   String?
  date          DateTime @db.Date
  count         Int      @default(1)
  created_at    DateTime @default(now())

  company       company  @relation(fields: [company_id], references: [id], onDelete: Cascade)

  @@index([company_id, date])
}

model resource_usage {
  id            Int      @id @default(autoincrement())
  company_id    Int
  resource_type String
  usage_value   Float
  unit          String
  created_at    DateTime @default(now())

  company       company  @relation(fields: [company_id], references: [id], onDelete: Cascade)

  @@index([company_id, created_at])
}

// Update company model relations
model company {
  // ... existing fields ...

  // Add new relations
  user_activity    user_activity[]
  feature_usage    feature_usage[]
  growth_metrics   growth_metrics[]
  error_logs       error_logs[]
  resource_usage   resource_usage[]

  // ... existing relations ...
}

// Update user model relations
model user {
  // ... existing fields ...

  // Add new relations
  user_activity    user_activity[]

  // ... existing relations ...
}

// NextAuth.js Models remain the same...